"""FlashPoint edge detectors (D1-2): noise-adaptive thunder detection with
optional external anchoring (GLM / Xweather / camera flash).

Standalone-portable: numpy + scipy + soundfile only — no dashboard imports —
so this package can be dropped into the M2 ECR plugin fork unchanged.
"""
